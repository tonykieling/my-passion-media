class Product {
  constructor(name, weight, dimensions) {
    this.name       = name;
    this.weight     = weight;
    this.dimensions = dimensions;
  }

  add(name, weight, dimensions) {
    constr
  }
}